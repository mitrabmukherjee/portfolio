# SMTP configuration for contact form emails (Nodemailer)
# Copy this file to .env.local in the project root and fill in your values.
# You will receive an email at SMTP_USER when someone submits the contact form.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
